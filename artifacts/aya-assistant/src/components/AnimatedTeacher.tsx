/**
 * AnimatedTeacher — DEPRECATED
 *
 * The component has been superseded by AyaAvatar (unified AYA identity).
 * Only the TeacherState type is kept here for backwards-compatibility until
 * all remaining references are migrated.
 *
 * DO NOT render <AnimatedTeacher> anywhere. Use <AyaAvatar> instead.
 */

export type TeacherState = "idle" | "talking" | "happy" | "thinking" | "encouraging";
