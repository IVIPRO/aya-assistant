[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/aya-assistant build", "pnpm --filter @workspace/api-server build"]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
