Implement Adaptive Learning Engine Phase 1B only.

Work in ONE pass.
Stay in Lite mode.
Do NOT switch to Autonomous Agent.
Do NOT redesign UI.
Do NOT modify unrelated files.
Do NOT add new routes.
Do NOT change Bulgarian lesson flow yet.
Do NOT add parent UI.
Do NOT add teacher UI.
Do NOT refactor the whole chat handler.

GOAL
Wire the existing adaptive profile data layer into the current math flow only.

SCOPE
1) Use the existing adaptive profile utility created in Phase 1A.
2) Modify only the current math task generation / math validation flow.
3) Keep all existing math behavior intact:
- addition
- subtraction
- multiplication
- division
- typed answers
- voice answers
- retry / hint logic
- follow-up next-task logic

IMPLEMENT ONLY
A. On every math answer evaluation:
- call updateAdaptiveProfile(childId, subject="mathematics", topic=currentMathOperation, wasCorrect)

B. Before generating the next math task:
- read profile with getAdaptiveProfile(childId)
- compute recommended difficulty with getRecommendedDifficulty(profile, "mathematics")
- compute adaptive mode with getAdaptiveMode(profile)

C. Apply deterministic math adaptation:
- if mode = "boost", generate next task at +1 difficulty relative to current level, capped safely
- if mode = "review", prefer easier difficulty or repeat weak operation/topic
- if mode = "normal", keep current level

D. Weak-topic review rule for math:
- if shouldReviewWeakTopic(profile, "mathematics") is true,
  prefer the weak operation for the next generated task
  instead of random operation selection
- but only inside math mode, never outside math mode

E. Preserve current stored active_question behavior.
Never break current validator.
Never use stale operation memory.
Always use current stored task.

TESTS
Add focused tests only for math adaptive wiring:
1) 3 correct math answers in a row increases next math difficulty
2) 2 wrong math answers in a row decreases next math difficulty
3) weak multiplication topic gets preferred on next math task
4) weak division topic gets preferred on next math task
5) review mode does not break exact answer validation
6) typed and voice math flows still work unchanged
7) non-math chat remains unchanged

IMPORTANT
- No Bulgarian lesson wiring yet
- No curriculum changes
- No subject router changes
- No UI work
- No broad cleanup refactor

STOP AFTER
- math adaptive wiring
- tests
- server clean

OUTPUT FORMAT
At the end report only:
1. files changed
2. exact math adaptive rules wired
3. tests added and passing count
4. exact next files for Phase 1C Bulgarian wiring